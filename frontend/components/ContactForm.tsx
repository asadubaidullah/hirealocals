"use client";
import { FormEvent,useState } from "react";
import { apiUrl } from "@/lib/site";
export default function ContactForm(){const [status,setStatus]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Sending…");const payload=Object.fromEntries(new FormData(e.currentTarget).entries());try{const r=await fetch(`${apiUrl}/api/contact`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});if(!r.ok)throw new Error();setStatus("Message received. Our team will get back to you.");e.currentTarget.reset()}catch{setStatus("Could not send right now. Please try again later.")}}return <form className="form-box" onSubmit={submit}><div className="form-grid"><div className="form-group"><label>Name</label><input name="name" required/></div><div className="form-group"><label>Email</label><input name="email" type="email" required/></div><div className="form-group full"><label>Subject</label><input name="subject" required/></div><div className="form-group full"><label>Message</label><textarea name="message" rows={7} required/></div><div className="full"><button className="btn">Send message</button></div>{status&&<div className="notice full">{status}</div>}</div></form>}

