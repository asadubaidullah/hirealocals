"use client";import Link from "next/link";import {FormEvent,useState} from "react";import {apiUrl} from "@/lib/site";export default function Page(){const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg('Sending…');const email=String(new FormData(e.currentTarget).get('email')||'');try{const r=await fetch(`${apiUrl}/api/auth/forgot-password`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMsg(d.message||d.detail||'Check your email.')}catch{setMsg('Could not connect to the API.')}}return <section className="section"><div className="container auth-page"><span className="eyebrow">Account recovery</span><h1>Forgot password?</h1><p className="lead">Enter your account email. If it is registered, we will send a secure reset link.</p><form className="form-box" onSubmit={submit}><div className="form-group"><label>Email</label><input name="email" type="email" required/></div><button className="btn" style={{width:'100%',marginTop:18}}>Send reset link</button>{msg&&<div className="notice" style={{marginTop:14}}>{msg}</div>}<p className="muted"><Link href="/login" className="admin-text-link">Back to login</Link></p></form></div></section>}
