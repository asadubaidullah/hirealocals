"use client";import Link from "next/link";import {useEffect,useState} from "react";import {apiUrl} from "@/lib/site";export default function Page(){const [msg,setMsg]=useState('Preparing verification…');const [ok,setOk]=useState(false);useEffect(()=>{const token=new URLSearchParams(window.location.search).get('token')||'';if(!token){setMsg('Verification token is missing.');return}(async()=>{try{const r=await fetch(`${apiUrl}/api/auth/verify-email`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});const d=await r.json();setOk(r.ok);setMsg(d.message||d.detail)}catch{setMsg('Could not connect to the API.')}})()},[]);return <section className="section"><div className="container auth-page"><span className="eyebrow">Email verification</span><h1>{ok?'Email verified':'Verify your email'}</h1><div className={`notice ${ok?'':'error'}`}>{msg}</div>{ok&&<p><Link href="/login" className="btn">Continue to login</Link></p>}</div></section>}
